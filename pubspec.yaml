name: conversor_numeros_mayas
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc_test: ^7.0.6
  cupertino_icons: ^1.0.0
  dartz: ^0.9.2
  flutter_bloc: ^6.0.6
  freezed_annotation: ^0.12.0
  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.2
  freezed: ^0.12.1
  json_serializable: ^3.5.0

flutter:

  uses-material-design: true
  assets:
    - assets/